#React Questions

- React is a javascript library and used to develop fast front end applications.
below is the cdn path of react and react dom liabraries.
<script crossorigin src="https://unpkg.com/react@18/umd/react.development.js"></script>
Above is the core file of React.
<script crossorigin src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
react-dom.development.js is used to modify DOM.

React is not a Framework, it uses javascript to run code/application.

const heading = React.createElement('h1', { id : 12}, 'Hello World from React!');

{} is used to add attributes to elements.
	
# What  React.createElement() is  returning ?
  It will returning an Object. It's creating an object.

# The order of the JS file is also important.
# What is crossorigin inside <script crossorigin src=”dddd”/>
-  Root.render replaces all code inside the root entity with new entity.

Difference between git and github ?